baseline:
  id: "2026-09-21_agentic-iam-hardening-phase3"
  purpose: "01_08 Phase 3: delegation-chain JWT claims. signAgentToken now embeds delegated_by (human operator or upstream agent). Live-verified: agent-a's real JWT shows delegated_by=local-operator, agent-c's shows delegated_by=agent-b, both confirmed accurate against the real delegations table. Backward-compatible, additive claim. Both test suites pass."
  captured_at: "2026-09-21T07:11:21Z"
  project: "editors_factory"
  previous_baseline: "2026-09-21_agentic-iam-hardening-phase2"
  git:
    commit: "983db2e259938317d47d715155a1ca23aaec8dcc"
    branch: "main"
    tag: ""
    dirty: true

runtime:
  type: "podman"
  architecture: "podman-compose, multi-stack (see prompts/base_project/01_01_factory_stack.md)"
  reachable: true

components:
  vault:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  infra:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  ollama:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  api:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  agents:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  ui:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true

verification:
  backend-health: PASS
  ollama-model: PASS
  mutating-scenarios: UNKNOWN

capture:
  overall: CAPTURED
  checks:
    source: CAPTURED
    runtime: CAPTURED
    components: CAPTURED
    verification: CAPTURED
