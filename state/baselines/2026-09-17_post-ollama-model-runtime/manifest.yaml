baseline:
  id: "2026-09-17_post-ollama-model-runtime"
  purpose: "prompts/base_project/04_01_ollama_model_runtime.md complete: shared factory-ollama runtime running healthy on factory-control with qwen3:4b-instruct, persistent factory-ollama_data model storage, idempotent model pull, documented API contract for agents A/B/C/D, and verified structured tool calling. Existing Vault HA cluster, Vault Agent, and PostgreSQL remain healthy. Corrected the state capture adapter's Vault container-name filter and removed stale initial-project boilerplate from generated summaries; prior immutable baselines were not modified."
  captured_at: "2026-09-17T10:45:32Z"
  project: "editors_factory"
  previous_baseline: "2026-09-17_post-postgres-dynamic-creds"
  git:
    commit: "5379ec86e072ab28c9f6c598b97926867934bf49"
    branch: "main"
    tag: ""
    dirty: true

runtime:
  type: "podman"
  architecture: "podman-compose, multi-stack (see prompts/base_project/01_01_factory_stack.md)"
  reachable: true

components:
  vault:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  infra:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  ollama:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  api:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: false
  agents:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: false
  ui:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: false

verification:
  backend-health: UNKNOWN
  ollama-model: PASS
  mutating-scenarios: UNKNOWN

capture:
  overall: CAPTURED
  checks:
    source: CAPTURED
    runtime: CAPTURED
    components: CAPTURED
    verification: CAPTURED
