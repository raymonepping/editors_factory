baseline:
  id: "2026-09-23_vault-agent-stall-recovery"
  purpose: "Fixed and live-verified: vault-agent's AppRole token silently stopped renewing after ~4 hours, causing every write route to fail with 'password authentication failed' while /api/health falsely reported ok. Forced fresh re-auth, recreated factory-api + agents, recovered a compounding vault-s seal/vault-1 unreachable issue from the recreate's broader-than-expected blast radius using make vault-unseal. Profile switch confirmed working live via Playwright; full backend suite 33/33."
  captured_at: "2026-09-23T16:10:11Z"
  project: "editors_factory"
  previous_baseline: "2026-09-23_sse-keepalive-fix"
  git:
    commit: "44deb989a41d2a6dc077326ffa24a9b38e5cc666"
    branch: "main"
    tag: ""
    dirty: true

runtime:
  type: "podman"
  architecture: "podman-compose, multi-stack (see prompts/base_project/01_01_factory_stack.md)"
  reachable: true

components:
  vault:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  infra:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  ollama:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  api:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  agents:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  ui:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true

verification:
  backend-health: PASS
  ollama-model: PASS
  mutating-scenarios: UNKNOWN

capture:
  overall: CAPTURED
  checks:
    source: CAPTURED
    runtime: CAPTURED
    components: CAPTURED
    verification: CAPTURED
