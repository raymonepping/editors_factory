baseline:
  id: "2026-09-21_agentic-iam-hardening-phase2"
  purpose: "01_08 Phase 2: task-scoped Sentinel check. require-agent-c-for-db-creds now also requires factory_task metadata to be present and non-empty on the child token (verified live: denied without it, allowed with it, using factory-api's own real Vault Agent token as parent). Extended vault-audit-crosscheck.py to compare task_id between the app's credential_events and Vault's own audit log, since Sentinel itself can only check presence, not correctness. Live-verified end to end with a real BAD run. Both test suites pass."
  captured_at: "2026-09-21T07:04:23Z"
  project: "editors_factory"
  previous_baseline: "2026-09-21_agentic-iam-hardening-phase1"
  git:
    commit: "c87fa66dc8880bbfb0a9e8b642f778104d650b42"
    branch: "main"
    tag: ""
    dirty: true

runtime:
  type: "podman"
  architecture: "podman-compose, multi-stack (see prompts/base_project/01_01_factory_stack.md)"
  reachable: true

components:
  vault:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  infra:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  ollama:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  api:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  agents:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true
  ui:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: true

verification:
  backend-health: PASS
  ollama-model: PASS
  mutating-scenarios: UNKNOWN

capture:
  overall: CAPTURED
  checks:
    source: CAPTURED
    runtime: CAPTURED
    components: CAPTURED
    verification: CAPTURED
