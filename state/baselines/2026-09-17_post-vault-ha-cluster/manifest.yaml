baseline:
  id: "2026-09-17_post-vault-ha-cluster"
  purpose: "prompts/base_project/02_01_vault_ha_cluster.md complete: compose/vault/compose.yaml + vault-s/vault-1/2/3 HCL configs + Makefile targets in place. Cluster not yet bootstrapped — Vault Enterprise license files (vault-s/config/*.hclic) are not present locally; vault-prepare.sh correctly refuses to proceed without fabricating them."
  captured_at: "2026-09-17T09:29:00Z"
  project: "editors_factory"
  previous_baseline: "2026-09-17_post-factory-stack"
  git:
    commit: "5379ec86e072ab28c9f6c598b97926867934bf49"
    branch: "main"
    tag: ""
    dirty: true

runtime:
  type: "podman"
  architecture: "podman-compose, multi-stack (see prompts/base_project/01_01_factory_stack.md)"
  reachable: true

components:
  vault:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: false
  infra:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: false
  ollama:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: false
  api:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: false
  agents:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: false
  ui:
    capture_status: CAPTURED
    compose_file_present: true
    observed_running: false

verification:
  backend-health: UNKNOWN
  mutating-scenarios: UNKNOWN

capture:
  overall: CAPTURED
  checks:
    source: CAPTURED
    runtime: CAPTURED
    components: CAPTURED
    verification: CAPTURED
