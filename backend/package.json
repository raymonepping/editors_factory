{
  "name": "factory-api",
  "version": "0.1.0",
  "private": true,
  "description": "The Factory — backend orchestrator, policy engine, and Vault credential broker.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
